const val = 10;
var emptyVal;

// Double negation (!!) will convert any object to boolean and will return false if nothing exists.
console.log(`Check if val exist: ${!!val}`);
console.log(`Check if emptyVal exist: ${!!emptyVal}`);
